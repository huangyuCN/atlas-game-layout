// Code generated by protoc-gen-atlas-actor. DO NOT EDIT.
// versions:
// - protoc-gen-atlas-actor v0.1.0
// - protoc             v5.26.1
// source: api/battle/v1/battle_service.proto

package battlev1

import (
	context "context"
	fmt "fmt"
	core "github.com/huangyuCN/atlas/contrib/actor/core"
	types "github.com/huangyuCN/atlas/contrib/actor/types"
	errors "github.com/huangyuCN/atlas/errors"
	proto "google.golang.org/protobuf/proto"
)

// BattleServiceActorServer 是业务 actor 实现的接口；签名由 service 块决定，
// returns 具体消息即 Ask，returns google.protobuf.Empty 即 Tell（单向）。
type BattleServiceActorServer interface {
	JoinBattle(ctx core.ActorContext, req *JoinBattleReq) (*JoinBattleReply, error)
	SendFrameInput(ctx core.ActorContext, msg *FrameInputReq) error
	SyncFrames(ctx core.ActorContext, req *SyncFramesReq) (*SyncFramesReply, error)
	Create(ctx core.ActorContext, req *CreateBattleRequest) (*CreateBattleReply, error)
	GetState(ctx core.ActorContext, req *GetStateReq) (*GetStateReply, error)
}

// UnimplementedBattleServiceActorServer 是兜底基类；业务 actor embed 它获得接口演进安全。
type UnimplementedBattleServiceActorServer struct{}

func (UnimplementedBattleServiceActorServer) JoinBattle(core.ActorContext, *JoinBattleReq) (*JoinBattleReply, error) {
	return nil, errors.InternalServer("ACTOR_METHOD_UNIMPLEMENTED", "actor 方法未实现: BattleService.JoinBattle")
}
func (UnimplementedBattleServiceActorServer) SendFrameInput(core.ActorContext, *FrameInputReq) error {
	return errors.InternalServer("ACTOR_METHOD_UNIMPLEMENTED", "actor 方法未实现: BattleService.SendFrameInput")
}
func (UnimplementedBattleServiceActorServer) SyncFrames(core.ActorContext, *SyncFramesReq) (*SyncFramesReply, error) {
	return nil, errors.InternalServer("ACTOR_METHOD_UNIMPLEMENTED", "actor 方法未实现: BattleService.SyncFrames")
}
func (UnimplementedBattleServiceActorServer) Create(core.ActorContext, *CreateBattleRequest) (*CreateBattleReply, error) {
	return nil, errors.InternalServer("ACTOR_METHOD_UNIMPLEMENTED", "actor 方法未实现: BattleService.Create")
}
func (UnimplementedBattleServiceActorServer) GetState(core.ActorContext, *GetStateReq) (*GetStateReply, error) {
	return nil, errors.InternalServer("ACTOR_METHOD_UNIMPLEMENTED", "actor 方法未实现: BattleService.GetState")
}

// battleServiceDecodeTable 是 type_url → 类型化 Unmarshal 闭包的解码表（生成期静态绑定）。
var battleServiceDecodeTable = map[string]func([]byte) (proto.Message, error){
	"type.googleapis.com/battle.v1.JoinBattleReq": func(b []byte) (proto.Message, error) {
		m := new(JoinBattleReq)
		return m, proto.Unmarshal(b, m)
	},
	"type.googleapis.com/battle.v1.FrameInputReq": func(b []byte) (proto.Message, error) {
		m := new(FrameInputReq)
		return m, proto.Unmarshal(b, m)
	},
	"type.googleapis.com/battle.v1.SyncFramesReq": func(b []byte) (proto.Message, error) {
		m := new(SyncFramesReq)
		return m, proto.Unmarshal(b, m)
	},
	"type.googleapis.com/battle.v1.CreateBattleRequest": func(b []byte) (proto.Message, error) {
		m := new(CreateBattleRequest)
		return m, proto.Unmarshal(b, m)
	},
	"type.googleapis.com/battle.v1.GetStateReq": func(b []byte) (proto.Message, error) {
		m := new(GetStateReq)
		return m, proto.Unmarshal(b, m)
	},
}

// NewBattleServiceDecodeInbound 返回装配进 Props.DecodeInbound 的入站解码钩子：
// 集群入站 payload 在投递 handler 链前按 type_url 归一为具体消息对象。
func NewBattleServiceDecodeInbound() func(typeURL string, payload []byte) (any, error) {
	return func(typeURL string, payload []byte) (any, error) {
		decode, ok := battleServiceDecodeTable[typeURL]
		if !ok {
			return nil, fmt.Errorf("actor: 未知消息类型 %q", typeURL)
		}
		return decode(payload)
	}
}

// battleServiceDispatch 组装业务实现与本地路由兜底；生命周期经 DispatchBase 断言转发。
type battleServiceDispatch struct {
	*core.DispatchBase
	impl BattleServiceActorServer
}

// OnAsk 分发 Ask 消息：前置钩子（可选）→ 静态 type switch，未命中回落本地路由。
func (d *battleServiceDispatch) OnAsk(ctx core.ActorContext, req any) (any, error) {
	if err := d.BeforeAsk(ctx, req); err != nil {
		return nil, err
	}
	switch r := req.(type) {
	case *JoinBattleReq:
		return d.impl.JoinBattle(ctx, r)
	case *SyncFramesReq:
		return d.impl.SyncFrames(ctx, r)
	case *CreateBattleRequest:
		return d.impl.Create(ctx, r)
	case *GetStateReq:
		return d.impl.GetState(ctx, r)
	default:
		return d.FallbackAsk(ctx, req)
	}
}

// OnTell 分发 Tell 消息：前置钩子（可选）→ 静态 type switch，未命中回落本地路由。
func (d *battleServiceDispatch) OnTell(ctx core.ActorContext, msg any) error {
	if err := d.BeforeTell(ctx, msg); err != nil {
		return err
	}
	switch m := msg.(type) {
	case *FrameInputReq:
		return d.impl.SendFrameInput(ctx, m)
	default:
		return d.FallbackTell(ctx, msg)
	}
}

// NewBattleServiceActorServer 把业务实现组装为 core.Handler（含本地路由与入站解码能力）。
func NewBattleServiceActorServer(impl BattleServiceActorServer, opts ...core.DispatchOption) core.Handler {
	return &battleServiceDispatch{
		DispatchBase: core.NewDispatchBase(impl, opts...),
		impl:         impl,
	}
}

// BattleServiceClusterClient 是**集群内部互调专用**的 client stub：强类型返回，
// 跨节点 []byte 解码在内部完成。跨服务/跨集群调用请走 RPC 面（本 proto 生成的 BattleServiceServer 接入层），也不要与客户端 SDK 的 BattleServiceOpClient 混用。
type BattleServiceClusterClient struct {
	inv core.ActorInvoker
}

// NewBattleServiceClusterClient 以发送端运行时构造集群互调 client stub。
func NewBattleServiceClusterClient(inv core.ActorInvoker) *BattleServiceClusterClient {
	return &BattleServiceClusterClient{inv: inv}
}

// JoinBattle 同步请求 Ask；业务错误以 Go error 返回（code/reason 经集群往返保留；
// 变参透传投递选项——sender/观测头等由调用方按需注入）。
func (c *BattleServiceClusterClient) JoinBattle(ctx context.Context, pid types.PID, req *JoinBattleReq, opts ...core.SendOption) (*JoinBattleReply, error) {
	rep, err := c.inv.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	switch v := rep.(type) {
	case *JoinBattleReply:
		return v, nil
	case []byte:
		out := new(JoinBattleReply)
		if err := proto.Unmarshal(v, out); err != nil {
			return nil, fmt.Errorf("actor: 响应解码失败: %w", err)
		}
		return out, nil
	default:
		return nil, fmt.Errorf("actor: 不支持的响应类型 %T", rep)
	}
}

// SendFrameInput 单向投递 Tell 消息（无回执；变参透传投递选项——sender/观测头等）。
func (c *BattleServiceClusterClient) SendFrameInput(ctx context.Context, pid types.PID, msg *FrameInputReq, opts ...core.SendOption) error {
	return c.inv.Tell(ctx, pid, msg, opts...)
}

// SyncFrames 同步请求 Ask；业务错误以 Go error 返回（code/reason 经集群往返保留；
// 变参透传投递选项——sender/观测头等由调用方按需注入）。
func (c *BattleServiceClusterClient) SyncFrames(ctx context.Context, pid types.PID, req *SyncFramesReq, opts ...core.SendOption) (*SyncFramesReply, error) {
	rep, err := c.inv.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	switch v := rep.(type) {
	case *SyncFramesReply:
		return v, nil
	case []byte:
		out := new(SyncFramesReply)
		if err := proto.Unmarshal(v, out); err != nil {
			return nil, fmt.Errorf("actor: 响应解码失败: %w", err)
		}
		return out, nil
	default:
		return nil, fmt.Errorf("actor: 不支持的响应类型 %T", rep)
	}
}

// Create 同步请求 Ask；业务错误以 Go error 返回（code/reason 经集群往返保留；
// 变参透传投递选项——sender/观测头等由调用方按需注入）。
func (c *BattleServiceClusterClient) Create(ctx context.Context, pid types.PID, req *CreateBattleRequest, opts ...core.SendOption) (*CreateBattleReply, error) {
	rep, err := c.inv.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	switch v := rep.(type) {
	case *CreateBattleReply:
		return v, nil
	case []byte:
		out := new(CreateBattleReply)
		if err := proto.Unmarshal(v, out); err != nil {
			return nil, fmt.Errorf("actor: 响应解码失败: %w", err)
		}
		return out, nil
	default:
		return nil, fmt.Errorf("actor: 不支持的响应类型 %T", rep)
	}
}

// GetState 同步请求 Ask；业务错误以 Go error 返回（code/reason 经集群往返保留；
// 变参透传投递选项——sender/观测头等由调用方按需注入）。
func (c *BattleServiceClusterClient) GetState(ctx context.Context, pid types.PID, req *GetStateReq, opts ...core.SendOption) (*GetStateReply, error) {
	rep, err := c.inv.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	switch v := rep.(type) {
	case *GetStateReply:
		return v, nil
	case []byte:
		out := new(GetStateReply)
		if err := proto.Unmarshal(v, out); err != nil {
			return nil, fmt.Errorf("actor: 响应解码失败: %w", err)
		}
		return out, nil
	default:
		return nil, fmt.Errorf("actor: 不支持的响应类型 %T", rep)
	}
}
