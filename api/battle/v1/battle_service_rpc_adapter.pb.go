// Code generated by protoc-gen-atlas-actor. DO NOT EDIT.
// versions:
// - protoc-gen-atlas-actor v0.1.0
// - protoc             v5.26.1
// source: api/battle/v1/battle_service.proto

package battlev1

import (
	context "context"
	core "github.com/huangyuCN/atlas/contrib/actor/core"
	relay "github.com/huangyuCN/atlas/contrib/actor/relay"
	errors "github.com/huangyuCN/atlas/errors"
	metadata "google.golang.org/grpc/metadata"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// BattleServiceRPCAdapter 把 gRPC 的 BattleService 调用接入 actor 面：
// 解析调用方 metadata（身份 / 发起者 / 幂等键）→ 按路由条目组装目标 PID → Ask/Tell。
// 实现同包 gRPC 接口 BattleServiceServer，可直接注册到 grpc.Server。
type BattleServiceRPCAdapter struct {
	// 嵌入 gRPC 兜底基类：满足接口的 mustEmbed 前向兼容约定（新增 rpc 时本接入层不实现也能编译）。
	UnimplementedBattleServiceServer
	rt   core.ActorInvoker
	opts relay.RPCAdapterOptions
}

// NewBattleServiceRPCAdapter 以集群发送端构造接入层（返回 gRPC 服务接口，便于直接注册）。
func NewBattleServiceRPCAdapter(rt core.ActorInvoker, opts relay.RPCAdapterOptions) BattleServiceServer {
	return &BattleServiceRPCAdapter{rt: rt, opts: opts}
}

// JoinBattle 实现 BattleServiceServer（returns 具体消息即 Ask）。
func (a *BattleServiceRPCAdapter) JoinBattle(ctx context.Context, req *JoinBattleReq) (*JoinBattleReply, error) {
	entry := BattleServiceRouteTable["/battle.v1.BattleService/JoinBattle"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /battle.v1.BattleService/JoinBattle: "+err.Error())
	}
	out, ok := resp.(*JoinBattleReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /battle.v1.BattleService/JoinBattle")
	}
	return out, nil
}

// SendFrameInput 实现 BattleServiceServer（returns Empty 即单向 Tell）。
func (a *BattleServiceRPCAdapter) SendFrameInput(ctx context.Context, req *FrameInputReq) (*emptypb.Empty, error) {
	entry := BattleServiceRouteTable["/battle.v1.BattleService/SendFrameInput"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	if err := a.rt.Tell(ctx, pid, req, opts...); err != nil {
		return nil, err
	}
	return &emptypb.Empty{}, nil
}

// SyncFrames 实现 BattleServiceServer（returns 具体消息即 Ask）。
func (a *BattleServiceRPCAdapter) SyncFrames(ctx context.Context, req *SyncFramesReq) (*SyncFramesReply, error) {
	entry := BattleServiceRouteTable["/battle.v1.BattleService/SyncFrames"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /battle.v1.BattleService/SyncFrames: "+err.Error())
	}
	out, ok := resp.(*SyncFramesReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /battle.v1.BattleService/SyncFrames")
	}
	return out, nil
}

// Create 实现 BattleServiceServer（returns 具体消息即 Ask）。
func (a *BattleServiceRPCAdapter) Create(ctx context.Context, req *CreateBattleRequest) (*CreateBattleReply, error) {
	entry := BattleServiceRouteTable["/battle.v1.BattleService/Create"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /battle.v1.BattleService/Create: "+err.Error())
	}
	out, ok := resp.(*CreateBattleReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /battle.v1.BattleService/Create")
	}
	return out, nil
}

// GetState 实现 BattleServiceServer（returns 具体消息即 Ask）。
func (a *BattleServiceRPCAdapter) GetState(ctx context.Context, req *GetStateReq) (*GetStateReply, error) {
	entry := BattleServiceRouteTable["/battle.v1.BattleService/GetState"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /battle.v1.BattleService/GetState: "+err.Error())
	}
	out, ok := resp.(*GetStateReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /battle.v1.BattleService/GetState")
	}
	return out, nil
}
