// Code generated by protoc-gen-atlas-actor. DO NOT EDIT.
// versions:
// - protoc-gen-atlas-actor v0.1.0
// - protoc             v5.26.1
// source: api/game/v1/player_service.proto

package gamev1

import (
	context "context"
	core "github.com/huangyuCN/atlas/contrib/actor/core"
	relay "github.com/huangyuCN/atlas/contrib/actor/relay"
	errors "github.com/huangyuCN/atlas/errors"
	metadata "google.golang.org/grpc/metadata"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// PlayerServiceRPCAdapter 把 gRPC 的 PlayerService 调用接入 actor 面：
// 解析调用方 metadata（身份 / 发起者 / 幂等键）→ 按路由条目组装目标 PID → Ask/Tell。
// 实现同包 gRPC 接口 PlayerServiceServer，可直接注册到 grpc.Server。
type PlayerServiceRPCAdapter struct {
	// 嵌入 gRPC 兜底基类：满足接口的 mustEmbed 前向兼容约定（新增 rpc 时本接入层不实现也能编译）。
	UnimplementedPlayerServiceServer
	rt   core.ActorInvoker
	opts relay.RPCAdapterOptions
}

// NewPlayerServiceRPCAdapter 以集群发送端构造接入层（返回 gRPC 服务接口，便于直接注册）。
func NewPlayerServiceRPCAdapter(rt core.ActorInvoker, opts relay.RPCAdapterOptions) PlayerServiceServer {
	return &PlayerServiceRPCAdapter{rt: rt, opts: opts}
}

// Register 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) Register(ctx context.Context, req *RegisterReq) (*RegisterReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/Register"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/Register: "+err.Error())
	}
	out, ok := resp.(*RegisterReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/Register")
	}
	return out, nil
}

// GetPlayerData 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) GetPlayerData(ctx context.Context, req *GetPlayerDataReq) (*PlayerDataReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/GetPlayerData"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/GetPlayerData: "+err.Error())
	}
	out, ok := resp.(*PlayerDataReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/GetPlayerData")
	}
	return out, nil
}

// GetBackpack 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) GetBackpack(ctx context.Context, req *GetBackpackReq) (*BackpackReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/GetBackpack"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/GetBackpack: "+err.Error())
	}
	out, ok := resp.(*BackpackReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/GetBackpack")
	}
	return out, nil
}

// EnterMatchQueue 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) EnterMatchQueue(ctx context.Context, req *EnterMatchQueueReq) (*EnterMatchQueueReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/EnterMatchQueue"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/EnterMatchQueue: "+err.Error())
	}
	out, ok := resp.(*EnterMatchQueueReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/EnterMatchQueue")
	}
	return out, nil
}

// CancelMatch 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) CancelMatch(ctx context.Context, req *CancelMatchReq) (*CancelMatchReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/CancelMatch"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/CancelMatch: "+err.Error())
	}
	out, ok := resp.(*CancelMatchReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/CancelMatch")
	}
	return out, nil
}

// GetMatchStatus 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) GetMatchStatus(ctx context.Context, req *GetMatchStatusReq) (*MatchStatusReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/GetMatchStatus"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/GetMatchStatus: "+err.Error())
	}
	out, ok := resp.(*MatchStatusReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/GetMatchStatus")
	}
	return out, nil
}

// CreateParty 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) CreateParty(ctx context.Context, req *CreatePartyReq) (*PartyReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/CreateParty"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/CreateParty: "+err.Error())
	}
	out, ok := resp.(*PartyReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/CreateParty")
	}
	return out, nil
}

// JoinParty 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) JoinParty(ctx context.Context, req *JoinPartyReq) (*PartyReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/JoinParty"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/JoinParty: "+err.Error())
	}
	out, ok := resp.(*PartyReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/JoinParty")
	}
	return out, nil
}

// LeaveParty 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) LeaveParty(ctx context.Context, req *LeavePartyReq) (*PartyReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/LeaveParty"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/LeaveParty: "+err.Error())
	}
	out, ok := resp.(*PartyReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/LeaveParty")
	}
	return out, nil
}

// GetParty 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) GetParty(ctx context.Context, req *GetPartyReq) (*PartyReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/GetParty"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/GetParty: "+err.Error())
	}
	out, ok := resp.(*PartyReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/GetParty")
	}
	return out, nil
}

// QueueParty 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) QueueParty(ctx context.Context, req *QueuePartyReq) (*PartyQueueReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/QueueParty"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/QueueParty: "+err.Error())
	}
	out, ok := resp.(*PartyQueueReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/QueueParty")
	}
	return out, nil
}

// Login 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) Login(ctx context.Context, req *LoginReq) (*LoginReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/Login"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/Login: "+err.Error())
	}
	out, ok := resp.(*LoginReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/Login")
	}
	return out, nil
}

// Logout 实现 PlayerServiceServer（returns Empty 即单向 Tell）。
func (a *PlayerServiceRPCAdapter) Logout(ctx context.Context, req *LogoutMsg) (*emptypb.Empty, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/Logout"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	if err := a.rt.Tell(ctx, pid, req, opts...); err != nil {
		return nil, err
	}
	return &emptypb.Empty{}, nil
}

// GrantItem 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) GrantItem(ctx context.Context, req *GrantItemReq) (*GrantItemReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/GrantItem"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/GrantItem: "+err.Error())
	}
	out, ok := resp.(*GrantItemReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/GrantItem")
	}
	return out, nil
}

// GetPlayer 实现 PlayerServiceServer（returns 具体消息即 Ask）。
func (a *PlayerServiceRPCAdapter) GetPlayer(ctx context.Context, req *GetPlayerReq) (*PlayerReply, error) {
	entry := PlayerServiceRouteTable["/game.v1.PlayerService/GetPlayer"]
	md, _ := metadata.FromIncomingContext(ctx)
	pid, err := relay.ResolveRequestPID(md, entry, req)
	if err != nil {
		// 身份/路由类错误映射为结构化错误：裸 error 过 gRPC 会变 codes.Unknown，
		// 客户端拿不到可判定的 reason。
		return nil, relay.MapError(err)
	}
	opts, err := relay.SendOptionsFromMetadata(md, entry, a.opts.SendOptions...)
	if err != nil {
		return nil, relay.MapError(err)
	}
	rep, err := a.rt.Ask(ctx, pid, req, opts...)
	if err != nil {
		return nil, err
	}
	// 回执可能是同进程的具体消息，也可能是跨节点的 []byte（wire 形态）——
	// 与网关透传同一口径：交给路由条目的 ReplyOf 归一解码，不硬断言类型。
	resp, err := entry.ReplyOf(rep)
	if err != nil {
		return nil, errors.InternalServer(relay.ReasonReplyDecodeFailed, "接入层回执解码失败: /game.v1.PlayerService/GetPlayer: "+err.Error())
	}
	out, ok := resp.(*PlayerReply)
	if !ok {
		return nil, errors.InternalServer(relay.ReasonReplyTypeMismatch, "接入层回执类型不符: /game.v1.PlayerService/GetPlayer")
	}
	return out, nil
}
